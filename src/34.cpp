#include <iostream>
using namespace std;

int main() {
    int x = 5;
    cout << "The value of x is: " << x << endl;

    if (x > 0) {
        cout << "x is greater than zero." << endl;
    } else {
        cout << "x is not greater than zero." << endl;
    }

    return 0;
}
